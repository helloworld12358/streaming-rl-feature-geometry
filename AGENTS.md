# Agent instructions

This repository implements a non-deep, strictly streaming reinforcement-learning course project. Do not add replay buffers, minibatch training, neural-network frameworks, credentials, or large raw result artifacts. Full experiments must be guarded by `RL_RUN_CONTEXT=remote` or `--allow-full-run`.
